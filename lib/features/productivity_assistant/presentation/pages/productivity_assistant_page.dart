import 'package:flutter/material.dart';

class ProductivityAssistantPage extends StatelessWidget {
  const ProductivityAssistantPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Productivity Assistant')),
      body: const Center(
        child: Text('Productivity Assistant Page - Coming Soon'),
      ),
    );
  }
}
